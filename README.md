# ECSE429-Project

### Tips to Run

- Helpful to use `@BeforeAll` method in JUnit to ensure the server is responding before tests start

- Run `mvn test` on terminal in root (where pom.xml is) or just run
